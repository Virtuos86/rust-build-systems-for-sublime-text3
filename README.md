# rust-build-systems-for-sublime-text3
A collection of build commands for compiling and running Rust code in Sublime Text 3.

All build scripts must be in the directory `~/.config/sublime-text-3/Packages/User`.
